# Shared outputs — copy to team/outputs.env and fill in as resources are created.
# Do NOT commit outputs.env (it is gitignored).

AWS_REGION=us-west-2
S3_BUCKET=enron-org-memory-data
S3_PREFIX=emails/

# Person 1 fills these after KB creation
KNOWLEDGE_BASE_ID=RW2OWNITJR
# AGENT_ID=              # Only if using Option B
# AGENT_ALIAS_ID=        # Only if using Option B

# Person 2 fills these after Lambda + API Gateway deploy
LAMBDA_FUNCTION_NAME=enron-query
API_GATEWAY_ASK_URL=
